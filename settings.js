// General
// -------------------------------------
const subjectName = "Informatik WF10 (Dt)";
const courseName = "Einstieg in JavaScript";
const showDebugLogs = true;
const hintTimeout = 10; // Time in seconds before the next hint button is enabled
const useFormalLanguage = false; // If true, "Sie" is used instead of "Du"

// Feedback
// -------------------------------------

const askForFeedback = false; // If set to true, each exercise will ask for feedback after it is completed

// Layout
// -------------------------------------

// Programming language (see src/highlight/languages))
const language = "python";

// Code snippet editor theme (see src/highlight/styles)
const theme = "github";
const showLineNumbers = true;
const languageNames = {
  python: "Python",
  javascript: "JavaScript",
  html: "HTML",
  css: "CSS",
  bash: "Bash",
  plaintext: "Text",
};
